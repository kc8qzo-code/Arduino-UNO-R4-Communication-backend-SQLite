info:
  name: Retrieve Sensor Readings
  type: http
  seq: 2

http:
  method: GET
  url: "{{environmentUrl}}/api/sensors/readings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
